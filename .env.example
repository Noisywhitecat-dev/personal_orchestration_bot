# Server port (default 3080)
PORT=3080
# SQLite database path (default ./data/orchestration.db)
DATABASE_PATH=./data/orchestration.db
# Maximum review rounds before escalating to the user (default 2)
MAX_REVIEW_ROUNDS=2
